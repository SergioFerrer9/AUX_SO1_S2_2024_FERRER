#GRAFANA
docker build -t server-node3 .
docker run -d --name=grafana -p 3000:3000 grafana/grafana
docker build -t sergioferrer9/server-node:1.0 .

#NETWORK
docker network create my_network
docker network ls
docker network connect my_network server-node2
docker run -d --network my_network -p 3000:3000 --name grafana grafana/grafana

#VOLUMEN
docker run --rm -v json_data:/data -v $(pwd):/local ubuntu bash -c "cp /local/data.json /data/data.json"
docker build -t server-node3 .
docker run -d -v json_data:/data --name server-node3 -p 5000:5000 server-node3





